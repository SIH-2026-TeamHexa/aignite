import React from 'react';
import { motion } from 'framer-motion';
export const fadeUp={hidden:{opacity:0,y:12},visible:{opacity:1,y:0,transition:{duration:.28}}};
export const stagger={hidden:{},visible:{transition:{staggerChildren:.06}}};
export const today=new Date('2026-08-16'); export const daysToExpiry=d=>Math.ceil((new Date(d)-today)/86400000); export const risk=x=>{const d=daysToExpiry(x.expiry);return Math.min(100,Math.max(4,Math.round((d<=1?55:d<=2?43:d<=4?26:d<=7?12:2)+Math.max(0,(x.stock/x.daily)-d)*8+(x.stock>x.min*1.3?10:0))))}; export const level=s=>s>=85?'Critical':s>=65?'High':s>=35?'Moderate':'Low'; export const wasteGrid=[[4,7,9],[3,6,8],[4,8,10],[5,7,9],[5,8,12],[7,11,16],[8,14,22]]; export const days=['Monday','Tuesday','Wednesday','Thursday','Friday','Saturday','Sunday']; export const meals=['Breakfast','Lunch','Dinner']; export const fmt=n=>new Intl.NumberFormat('en-IN').format(Math.round(n));
export function Page({children}){return <motion.main className="page" initial={{opacity:0,y:8}} animate={{opacity:1,y:0}} transition={{duration:.35}}>{children}</motion.main>}; export function SectionTitle({title,subtitle,icon:Icon}){return <div className="section-title"><div>{Icon&&<span className="icon-title"><Icon size={21}/></span>}<div><span className="eyebrow">Kitchen intelligence</span><h1>{title}</h1><p>{subtitle}</p></div></div></div>}; export function Stat({label,value,sub,kind=''}){return <div className={'stat '+kind}><span>{label}</span><b>{value}</b>{sub&&<small>{sub}</small>}</div>}
